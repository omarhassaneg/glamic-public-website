import { LucideIcon } from "lucide-react";

export interface WebsiteFeature {
  icon: LucideIcon;
  title: string;
  description: string;
}

export interface PortfolioImage {
  src: string;
  alt: string;
}