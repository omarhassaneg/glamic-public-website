import { LucideIcon } from "lucide-react";

export interface LocatorFeature {
  icon: LucideIcon;
  title: string;
  description: string;
}

export interface ServiceArea {
  zone: string;
  fee: string;
}