import { LucideIcon } from "lucide-react";

export interface PaymentFeature {
  icon: LucideIcon;
  title: string;
  description: string;
}

export interface PaymentMethod {
  icon: LucideIcon;
  name: string;
}